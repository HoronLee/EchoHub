server:
  port: 8080
  host: "0.0.0.0"
  mode: "debug" # "release" or "debug"

database:
  type: "mysql"
  source: "echohub:echohub@tcp(serv.horon.top:3306)/echohub?parseTime=True&loc=Local"
  logmode: "release" # "release" or "debug"

auth:
  jwt:
    expires: 2592000 # 30天（单位秒）
    issuer: "echohub"
    audience: "echohub"
